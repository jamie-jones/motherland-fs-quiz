const Fixer = ({title, content}) => {
    return(
        <div>
        <h1>{title}</h1>
        <h4>{content}</h4>
        </div>
    )
}

const Necro = ({title, content}) => {
    return(
        <div>
            <h1>{title}</h1>
            <h4>{content}</h4>
        </div>
    )
}

const Knower = ({title, content}) => {
    return(
        <div>
            <h1>{title}</h1>
            <h4>{content}</h4>
        </div>
    )
}

const Blaster = ({title, content}) => {
    return(
        <div>
            <h1>{title}</h1>
            <h4>{content}</h4>
        </div>
    )
}

export {Fixer, Necro, Knower, Blaster}